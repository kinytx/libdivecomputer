# Legacy Python Prototype

These files are early Garmin FIT/BLE sidecar prototypes. They are archived
because the active implementation path now uses the C core and thin wrappers.

Keep them for temporary reference only. New work should target:

- `../../c`
- `../../ldc`
- `../../wasm`
- `../../wss`
