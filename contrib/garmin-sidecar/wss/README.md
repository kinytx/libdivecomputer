# WSS

Detailed WSS documentation now lives in:

```text
../docs/garmin-wss-protocol.md
```

`device_session_router.js` is the active router. `garmin_ble_wss_relay.js` is
kept as old Garmin-only glue for reference.
